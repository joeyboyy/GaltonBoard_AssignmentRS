To reproduce the results, create a build directory and call CMake to
compile the executable. Run it (from within the build folder!) to 
populate/overwrite the contents of the folder out/. To reproduce the plots (svg format), execute plot.plt with gnuplot.